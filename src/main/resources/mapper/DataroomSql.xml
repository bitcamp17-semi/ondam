<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.DataroomMapper">
    <!--특정 자료실(roomId)에 속한 파일 목록 조회 -->
    <select id="readFilesByRoomId" resultType="FilesDto">
        SELECT * FROM files
        WHERE (roomId = #{roomId} OR #{roomId} = 0)
          AND (title LIKE CONCAT('%', #{keyword}, '%')
            OR comment LIKE CONCAT('%', #{keyword}, '%')
            OR name LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY createdAt DESC
            LIMIT #{offset}, #{limit};
    </select>
    <!--ID로 찾기-->
    <select id="readById" resultType="FilesDto">
        SELECT * FROM files
        WHERE id = #{id}
    </select>

    <!-- 자료실 목록 조회 -->
    <select id="readCategories" resultType="DataroomDto">
        SELECT DISTINCT id, name
        FROM data_room
        ORDER BY name
    </select>

    <!--파일 저장 -->
    <insert id="createFile">
        INSERT INTO files (title, comment, roomId, name, path, authorId, type, createdAt,updatedAt)
        VALUES (#{title}, #{comment}, #{roomId}, #{name}, #{path}, #{authorId}, #{type}, NOW(),NOW())
    </insert>
    <!--삭제-->
    <delete id="deleteById">
        DELETE FROM files
        WHERE id = #{id}
    </delete>
</mapper>