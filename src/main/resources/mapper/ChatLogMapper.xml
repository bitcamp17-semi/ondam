<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.ChatLogMapper">

	<!-- C -->
	<insert id="createChatLog" parameterType="ChatLogDto">
		insert into chat_log (groupid, senderid, message, createdat)
		values (#{groupId}, #{senderId}, #{message}, #{createdAt})
	</insert>

	<!-- R -->
	<select id="readChatLogsByGroupId" resultType="ChatLogDto"
		parameterType="int">
		select * from chat_log
		where groupid = #{groupId}
		order by createdat asc
	</select>	

	<!-- U -->
	<update id="updateChatLog" parameterType="ChatLogDto">
		update chat_log
		set message = #{message}
		where id = #{id}
	</update>

	<!-- D -->
	<delete id="deleteChatLog" parameterType="long">
		delete from chat_log
		where id = #{id}
	</delete>
</mapper>
