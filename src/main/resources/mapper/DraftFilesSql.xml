<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.DraftFilesMapper">
    <insert id="createFiles" parameterType="DraftFilesDto">
        insert into draft_files (name, draftId, path, createdAt)
        values (#{name}, #{draftId}, #{path}, now())
    </insert>
    <select id="readFilesByDraft" parameterType="int" resultType="DraftFilesDto">
        select *
        from draft_files
        where draftId = #{draftId}
    </select>
    <select id="readFileById" resultType="DraftFilesDto" parameterType="int">
        select *
        from draft_files
        where id = #{id}
    </select>
</mapper>